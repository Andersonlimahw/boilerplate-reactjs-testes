

em docs/e2e atualize o arquivo <feature>.checklist.md com oque ja foi implementado, exemplo navigation.checklist.md 

# Sample Exec:

codex exec --full-auto -c model="gpt-5" -c reasoning="high" -c temperature="0.77" -c max_tokens="777000" -c filesystem.write="true" -c allow.shell="true" -c allow.git="true" "prompt"

## Ee2
codex exec --full-auto -c model="gpt-5" -c reasoning="high" -c temperature="0.77" -c max_tokens="777000" -c filesystem.write="true" -c allow.shell="true" -c allow.git="true" "Leia os arquivos em docs/e2e/navigation.checklist.md e entao implemente os testes e2e utlizando a ferramenta https://playwright.dev, faça como um especialista em testes e desenvolvimento de software lembre-se de verificar meu codebase e implementacoes existentes"
